<br/>
<% flash.each do |name, msg| %>
    <div class="alert alert-warning" role="alert">
        <button class="close" data-dismiss="alert">
        <small>x</small>
        </button>
        <%= content_tag(:div, msg) %>
    </div>
<% end %>